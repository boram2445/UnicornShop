import styled from "styled-components";
import { ModalBackGround, ModalContainer, CloseBtn } from "../../common/Modal/modalStyle";

const ModalWrapper = styled(ModalContainer)`
  padding-top: 50px;
  width: 500px;
  height: 550px;
`;

export { ModalBackGround, ModalWrapper, CloseBtn };
